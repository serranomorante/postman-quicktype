describe("Testing", () => {
  it("my test works", () => {
    expect(1 + 1).toEqual(2);
  });
});

// const myConfig = {
//   paths: ["GEMS>Auth>Get current permissions", "GEMS>Auth>Get user options"],
// };
